# Addon Import

This repository contains the extracted contents of:

`MerchantMind_v1.4.5-no-ender-pearl.mcaddon`

The source package was downloaded from the Dropbox URL supplied for this import on 2026-09-09. The `.mcaddon` is a ZIP container with two nested `.mcpack` files, which were extracted into the repository as:

- `BP/` — behavior pack, including `scripts/main.js` and item definitions
- `RP/` — resource pack, including UI definitions, language files, textures, and pack metadata

The embedded pack manifests identify the package as Merchant Mind version `1.4.4`, target minimum Bedrock engine `1.21.0`, with `@minecraft/server` `1.13.0` and `@minecraft/server-ui` `1.2.0` dependencies. The repository README notes that the intended runtime is Minecraft Bedrock `1.26.40+`.

The original download is not committed; only the recursively extracted source files are tracked.
